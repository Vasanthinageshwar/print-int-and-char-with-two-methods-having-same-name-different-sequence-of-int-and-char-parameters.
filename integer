Create a class to print an integer and a character with two methods having the same name but different sequence of the integer

and the character parameters.
For example, if the parameters of the first method are of the form (int n, char c), then that of the second method will be of the form
(char c, int n).

CODE:
import java.util.*;
class AddAmount
{
void display(int i, char ch)
{
System.out.print(i+" ");
System.out.println(ch);
}
void display(char ch, int i)
{
System.out.print(ch+" ");
System.out.println(i);
}
}

class Que2_A3
{
public static void main(String args[])
{
AddAmount obj1 = new AddAmount();
Scanner sc=new Scanner(System.in);
int num=sc.nextInt();
char ch=sc.next().charAt(0);
obj1.display(num,ch);
obj1.display(ch,num);
}
}
